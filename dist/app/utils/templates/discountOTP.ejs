<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Your Tracksy Discount OTP</title>
    <style>
      body {
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        background-color: #f5f5f5;
        margin: 0;
        padding: 0;
        color: #333;
      }
      .container {
        max-width: 600px;
        margin: 30px auto;
        background-color: #ffffff;
        border-radius: 16px;
        box-shadow: 0 5px 20px rgba(0, 0, 0, 0.1);
        overflow: hidden;
      }
      .header {
        background: linear-gradient(135deg, #1e3a8a, #111827);
        text-align: center;
        padding: 40px 20px;
        color: #fff;
      }
      .header h1 {
        margin: 0;
        font-size: 26px;
        letter-spacing: 0.5px;
      }
      .header p {
        font-size: 15px;
        color: #d1d5db;
        margin-top: 10px;
      }
      .content {
        padding: 35px 25px;
        text-align: center;
      }
      .otp-box {
        display: inline-block;
        background: linear-gradient(135deg, #2563eb, #1e40af);
        color: #fff;
        padding: 18px 40px;
        font-size: 28px;
        font-weight: 700;
        letter-spacing: 4px;
        border-radius: 12px;
        margin: 20px 0;
      }
      .info {
        font-size: 16px;
        color: #555;
        line-height: 1.6;
        margin-bottom: 25px;
      }
      .discount {
        background-color: #f3f4f6;
        padding: 15px;
        border-radius: 12px;
        text-align: left;
        margin-bottom: 20px;
      }
      .discount p {
        margin: 5px 0;
        font-size: 15px;
      }
      .timer {
        font-size: 14px;
        color: #ef4444;
        margin-top: 10px;
        font-style: italic;
      }
      .btn {
        display: inline-block;
        background: linear-gradient(135deg, #4f46e5, #2563eb);
        color: #fff;
        padding: 12px 25px;
        border-radius: 25px;
        text-decoration: none;
        font-weight: 600;
        transition: background 0.3s;
      }
      .btn:hover {
        background: linear-gradient(135deg, #1d4ed8, #1e40af);
      }
      .footer {
        text-align: center;
        font-size: 13px;
        color: #777;
        padding: 20px;
        border-top: 1px solid #eee;
        background-color: #fafafa;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h1>Your Tracksy Discount Code 🎁</h1>
        <p>Exclusive offer just for you!</p>
      </div>
      <div class="content">
        <h2>Hello, Sir!</h2>
        <p class="info">
          We’re excited to share your special discount code. Use the OTP below
          to claim <b><%= discount %>% off</b> your next ride. Act fast — it
          expires in <b>1 hour</b>!
        </p>

        <div class="otp-box"><%= otp %></div>

        <div class="discount">
          <p><strong>Discount:</strong> 15% off your ride</p>
          <p><strong>Valid for:</strong> 1 hour from now</p>
        </div>

        <p class="timer">⚠️ This OTP will expire automatically after 1 hour.</p>
      </div>

      <div class="footer">
        <p>© <%= new Date().getFullYear() %> Tracksy. All rights reserved.</p>
      </div>
    </div>
  </body>
</html>
