<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Tracksy - Password Reset OTP</title>
    <style>
      body {
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        background-color: #0f172a;
        color: #e2e8f0;
        margin: 0;
        padding: 0;
      }

      .container {
        max-width: 600px;
        margin: 40px auto;
        background-color: #1e293b;
        border-radius: 20px;
        overflow: hidden;
        box-shadow: 0 8px 25px rgba(0, 0, 0, 0.4);
        border: 1px solid #334155;
      }

      .header {
        text-align: center;
        padding: 40px 20px 25px;
        background: linear-gradient(135deg, #1e3a8a, #0f172a);
      }

      .header img {
        width: 80px;
        height: 80px;
        border-radius: 14px;
        margin-bottom: 15px;
      }

      .header h1 {
        color: #ffffff;
        font-size: 24px;
        margin: 0;
      }

      .content {
        text-align: center;
        padding: 30px 25px;
      }

      .content p {
        font-size: 15px;
        color: #cbd5e1;
        line-height: 1.6;
        margin-bottom: 25px;
      }

      .otp-box {
        display: inline-block;
        background-color: #0f172a;
        border: 2px dashed #38bdf8;
        padding: 20px 35px;
        border-radius: 16px;
        margin-bottom: 25px;
        box-shadow: 0 0 10px rgba(56, 189, 248, 0.3);
      }

      .otp {
        font-size: 34px;
        letter-spacing: 10px;
        font-weight: bold;
        color: #38bdf8;
      }

      .info-box {
        background-color: #111827;
        padding: 15px;
        border-radius: 12px;
        margin-bottom: 20px;
        text-align: left;
      }

      .info-box p {
        margin: 6px 0;
        font-size: 14px;
        color: #e2e8f0;
      }

      .warning {
        background-color: rgba(239, 68, 68, 0.15);
        color: #f87171;
        padding: 12px;
        border-radius: 10px;
        font-size: 13px;
        margin-top: 15px;
      }

      .footer {
        text-align: center;
        font-size: 13px;
        color: #94a3b8;
        padding: 25px;
        background-color: #0f172a;
        border-top: 1px solid #1e293b;
      }

      @media (max-width: 600px) {
        .container {
          margin: 20px;
        }

        .otp {
          font-size: 28px;
          letter-spacing: 6px;
        }
      }
    </style>
  </head>

  <body>
    <div class="container">
      <div class="header">
        <img src="<%= logo %>" alt="Tracksy Logo" />
        <h1>Password Reset Request 🔐</h1>
      </div>

      <div class="content">
        <p>
          We received a request to reset the password for your
          <b>Tracksy</b> account. Use the One-Time Password (OTP) below to
          continue the process.
        </p>

        <div class="otp-box">
          <div class="otp"><%= otp %></div>
        </div>

        <p>This OTP will expire in <strong>2 minutes</strong>.</p>

        <div class="info-box">
          <p><strong>📧 Email:</strong> <%= email %></p>
        </div>

        <div class="warning">
          ⚠️ If you did not request a password reset, please ignore this message
          or contact our support team immediately.
        </div>
      </div>

      <div class="footer">
        <p>© <%= new Date().getFullYear() %> Tracksy. All rights reserved.</p>
        <p>Drive safe. Ride smart. 🚗</p>
      </div>
    </div>
  </body>
</html>
